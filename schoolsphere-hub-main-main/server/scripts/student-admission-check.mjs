const base=process.env.STUDENT_TEST_API_URL??"http://localhost:43132/api";
async function req(path,{token,method="GET",body}={}){const r=await fetch(`${base}${path}`,{method,headers:{...(body?{"Content-Type":"application/json"}:{}),...(token?{Authorization:`Bearer ${token}`}:{})},...(body?{body:JSON.stringify(body)}:{})});return{status:r.status,data:await r.json().catch(()=>null)};}
function ok(value,message){if(!value)throw new Error(`${message}`);console.log(`PASS ${message}`);}
async function login(email){const r=await req("/auth/login",{method:"POST",body:{email,password:"password123"}});ok(r.status===200,`login ${email}`);return r.data.token;}
const stamp=Date.now(),suffix=String(stamp).slice(-7),admissionPhone=`7${String(stamp).slice(-9)}`;const a=await login("everbright.admin@example.com"),b=await login("riverside.admin@example.com"),teacher=await login("everbright.teacher@example.com"),staff=await login("everbright.staff@example.com"),parentToken=await login("everbright.parent@example.com"),studentToken=await login("everbright.student@example.com"),superToken=await login("superadmin@example.com");
const classesA=(await req("/classes",{token:a})).data,classesB=(await req("/classes",{token:b})).data;const classA=classesA[0],classB=classesB[0];ok(classA.academic_year_id&&classB.academic_year_id,"academic placements available in both schools");
const parentA=(await req("/parents",{token:a})).data[0],parentB=(await req("/parents",{token:b})).data[0];
const admissionNo1=`ADM-STEP4-${stamp}-1`,admissionNo2=`ADM-STEP4-${stamp}-2`,roll1=1000000000+Number(suffix),roll2=roll1+1,email=`step4.guardian.${stamp}@example.test`;const p=await req("/parents",{token:a,method:"POST",body:{name:`Step4 Guardian ${stamp}`,email}});ok(p.status===201,"guardian create");ok((await req("/parents",{token:a,method:"POST",body:{name:"Duplicate",email}})).status===409,"duplicate guardian contact rejected in school");
const s1=await req("/students",{token:a,method:"POST",body:{name:`Step4 Child One ${stamp}`,admissionNo:admissionNo1,roll:roll1,classId:classA.id,academicYearId:classA.academic_year_id,parentId:p.data.id,dob:"2012-05-01",email:`child.one.${stamp}@example.test`}});ok(s1.status===201,"student create with academic placement and guardian");
ok((await req("/students",{token:b,method:"POST",body:{name:"Same Number Other School",admissionNo:admissionNo1,roll:roll1,classId:classB.id,academicYearId:classB.academic_year_id}})).status===201,"admission and roll identifiers are school scoped");
ok((await req("/students",{token:a,method:"POST",body:{name:"Duplicate Admission",admissionNo:admissionNo1}})).status===400,"duplicate admission number rejected within school");
ok((await req("/students",{token:a,method:"POST",body:{name:"Duplicate Roll",roll:roll1,classId:classA.id,academicYearId:classA.academic_year_id}})).status===409,"duplicate roll rejected in placement");
ok((await req("/students",{token:a,method:"POST",body:{name:"Cross Class",classId:classB.id,academicYearId:classB.academic_year_id}})).status===400,"cross-school class rejected");
ok((await req("/students",{token:a,method:"POST",body:{name:"Wrong Year",classId:classA.id,academicYearId:classB.academic_year_id}})).status===400,"wrong academic year rejected");
const s2=await req("/students",{token:a,method:"POST",body:{name:`Step4 Child Two ${stamp}`,admissionNo:admissionNo2,roll:roll2,classId:classA.id,academicYearId:classA.academic_year_id}});ok(s2.status===201,"second sibling created");ok((await req(`/parents/${p.data.id}/link`,{token:a,method:"POST",body:{studentId:s2.data.id,relationship:"Mother"}})).status===200,"guardian linked to second child");ok((await req(`/parents/${p.data.id}/link`,{token:a,method:"POST",body:{studentId:s2.data.id}})).status===409,"duplicate guardian relationship rejected");let children=await req(`/parents/${p.data.id}/children`,{token:a});ok(children.status===200&&children.data.length===2,"one guardian views multiple children");
ok((await req(`/parents/${p.data.id}/link`,{token:a,method:"POST",body:{studentId:(await req("/students",{token:b})).data[0].id}})).status===404,"cross-school guardian link rejected");
ok((await req(`/parents/${p.data.id}/unlink`,{token:a,method:"POST",body:{studentId:s1.data.id}})).status===200,"guardian unlink succeeds");children=await req(`/parents/${p.data.id}/children`,{token:a});ok(children.data.length===1&&children.data[0].id===s2.data.id,"unlink preserves sibling relationship");ok((await req(`/parents/${p.data.id}`,{token:a,method:"DELETE"})).status===409,"linked guardian delete blocked");
ok((await req(`/students/${s2.data.id}`,{token:a,method:"PATCH",body:{phone:"8888801234"}})).status===200,"student update persists");const searched=await req(`/students?q=${encodeURIComponent(`Step4 Child Two ${stamp}`)}`,{token:a});ok(searched.status===200&&searched.data.length===1&&searched.data[0].phone==="8888801234","backend student search and update readback");const filtered=await req(`/students?classId=${classA.id}&section=${encodeURIComponent(classA.section)}&status=Active`,{token:a});ok(filtered.status===200&&filtered.data.some(x=>x.id===s2.data.id)&&filtered.data.every(x=>x.school_id===classA.school_id),"backend class section status filters are school scoped");
const admission=await req("/admissions",{token:a,method:"POST",body:{applicant:`Step4 Admission Student ${stamp}`,dob:"2011-04-02",gender:"Female",classIdApplied:classA.id,academicYearId:classA.academic_year_id,parentName:"Admission Guardian",parentRelation:"Father",contactPhone:admissionPhone,stage:"APPLICATION"}});ok(admission.status===201,"admission application created");let id=admission.data.id;for(const stage of ["DOCUMENT_VERIFICATION","UNDER_REVIEW","APPROVED"]){const tr=await req(`/admissions/${id}/transition`,{token:a,method:"POST",body:{toStage:stage}});ok(tr.status===200,`admission transition to ${stage}`);}const converted=await req(`/admissions/${id}/convert`,{token:a,method:"POST"});ok(converted.status===201,"approved admission converts transactionally");ok((await req(`/admissions/${id}/convert`,{token:a,method:"POST"})).status!==201,"duplicate admission conversion rejected");const convertedStudent=await req(`/students/${converted.data.studentId}`,{token:a});ok(convertedStudent.status===200&&convertedStudent.data.class_id===classA.id&&convertedStudent.data.academic_year_id===classA.academic_year_id,"converted student placement persisted");
const listB=(await req("/students",{token:b})).data;ok((await req(`/students/${listB[0].id}`,{token:a})).status===404,"School A cannot GET School B student");ok((await req(`/students/${listB[0].id}`,{token:a,method:"PATCH",body:{name:"Blocked"}})).status===404,"School A cannot PATCH School B student");ok((await req(`/students/${listB[0].id}`,{token:a,method:"DELETE"})).status===404,"School A cannot DELETE School B student");ok((await req(`/parents/${parentB.id}`,{token:a})).status===404,"School A cannot GET School B parent");
const ownParent=(await req("/parents",{token:parentToken})).data[0];const ownChildren=await req(`/parents/${ownParent.id}/children`,{token:parentToken});ok(ownChildren.status===200&&ownChildren.data.length>0,"parent login sees linked children");ok((await req(`/students/${listB[0].id}`,{token:parentToken})).status===404,"parent direct-ID attack denied");const ownStudent=await req("/students/me",{token:studentToken});ok(ownStudent.status===200,"student reads own profile");ok((await req(`/students/${s2.data.id}`,{token:studentToken})).status===403,"student cannot use list/detail permission for another student");const studentGuardians=await req("/parents",{token:studentToken});ok(studentGuardians.status===200&&studentGuardians.data.length>0,"student lists own linked guardians");ok((await req(`/parents/${studentGuardians.data[0].id}`,{token:studentToken})).status===200,"student reads own linked guardian");ok((await req(`/parents/${parentB.id}`,{token:studentToken})).status===404,"student cannot read unrelated guardian");
ok((await req("/students",{token:teacher,method:"POST",body:{name:"Denied"}})).status===403,"teacher cannot create student");ok((await req("/students",{token:staff})).status===200,"authorized admin staff can list students");ok((await req("/students",{token:superToken})).status===403,"super admin is not implicitly school scoped");
const archive=await req(`/students/${converted.data.studentId}`,{token:a,method:"DELETE"});ok(archive.status===200&&archive.data.archived===true,"converted student archives instead of deleting referenced history");ok((await req(`/students/${converted.data.studentId}`,{token:a})).data.status==="Archived","archived student remains queryable and persisted");
console.log("Student, admission and guardian integration verification complete.");
